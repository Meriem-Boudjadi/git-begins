cloner un repository : 

git clone "url GitHub"


Les 3 commandes de push:

git add .
git commit -m ''
git push

commade qui permet de pull :
git pull 

pour crée une branche :
git branch < nom de la branche >

pour switcher de branche :
git checkout < nom de la branche >
